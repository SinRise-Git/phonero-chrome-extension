# Phonero++

A Chrome extension that adds extra functionality to Phonero.

## Features
- Add custom tabs to Phonero
- Save custom tabs to local storage

## Installation

### 1. Clone the repository and remove unnecessary files
Run the following commands in a terminal:

```bash
git clone https://github.com/SinRise-Git/Service-desk-chrome-extension.git
```
```bash
cd phonero-chrome-extension
```
```bash
rm README.md .gitignore
```
Remberer where you save the repository too.
## 2. Got to extentions in chrome  
Open Chrome and navigate to: <chrome://extensions/>
## 3. Activate developere mode 
Enable Developer mode by toggling the switch in the top-right corner.
## 4. Load the extension
1. Click Load unpacked in the top-left corner.e the file location where you cloned the repository. 
2. Navigate to the folder where you cloned the repository.
3. Select the project folder itself (open the folder, don’t just select it).
